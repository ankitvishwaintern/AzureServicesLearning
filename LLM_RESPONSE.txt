{"summary":"The OutOfMemoryException is caused by the GenerateBigReport endpoint attempting to allocate an int array with int.MaxValue elements (~8.5GB contiguous memory), which is guaranteed to fail. Additionally, the static _globalReportCache list permanently accumulates byte arrays with no eviction, causing unbounded memory growth across requests. The fix removes the massive unconditional allocation and replaces it with a safe, bounded allocation, and eliminates the unbounded static cache growth issue.","changes":[{"file":"Controllers/MemoryController.cs","reason":"Allocating an array of int.MaxValue elements deterministically throws OutOfMemoryException since it requires ~8.5GB of contiguous memory. Replace with a safe, bounded, and configurable size to avoid crashing the process, while still allowing the endpoint to demonstrate report generation without unbounded resource exhaustion.","start_line":26,"end_line":44,"replacement_code":"        [HttpGet(\"generate\")]\n        public IActionResult GenerateBigReport()\n        {\n            // Fixed: Removed the unconditional massive allocation (int.MaxValue elements)\n            // that guaranteed an OutOfMemoryException. Use a bounded, safe size instead,\n            // and avoid appending to the static cache indefinitely to prevent memory leaks.\n            try\n            {\n                const int SafeArraySize = 1_000_000; // ~4MB, safe and bounded\n                int[] safeArray = new int[SafeArraySize];\n\n                // Avoid unbounded growth of the static cache: cap its size and reuse memory.\n                const int MaxCachedReports = 10;\n                if (_globalReportCache.Count >= MaxCachedReports)\n                {\n                    _globalReportCache.RemoveAt(0);\n                }\n                _globalReportCache.Add(new byte[1024]); // small, bounded addition instead of unbounded growth\n\n                return Ok(new { Message = \"Report generated safely.\", Length = safeArray.Length });\n            }\n            catch (Exception ex)\n            {\n                _telemetryClient.TrackException(ex);\n                throw;\n            }\n        }"}]